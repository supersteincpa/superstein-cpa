import * as React from 'react'

import WhereWeLocated from './WhereWeLocated'

export default {
  title: 'Sections/WhereWeLocated',
  component: WhereWeLocated,
}

const Template = (args) => <WhereWeLocated {...args} />

export const Default = Template.bind({})
Default.args = {}
